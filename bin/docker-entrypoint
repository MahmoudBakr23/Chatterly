#!/bin/bash -e

# Create database if it doesn't exist, then run any pending migrations.
# db:prepare is idempotent — safe to run on every boot, handles first-run and deploys alike.
# Runs unconditionally so it works regardless of how the server is started.
./bin/rails db:prepare

exec "${@}"
